var config = {
  defaultLabels: []
};

config.defaultLabels.food = { color: 'red', spent: 0 };
config.defaultLabels.market = { color: 'green', spent: 0 };
config.defaultLabels.party = { color: 'blue', spent: 0 };

module.exports = config;
